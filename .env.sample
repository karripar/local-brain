PORT=3006
MILVUS_ADDRESS=localhost:19530
OLLAMA_HOST=http://localhost:11434
EMBEDDING_MODEL=nomic-embed-text
COMPLETION_MODEL=mistral
VECTOR_DIMENSIONS=768
COLLECTION_NAME=llama_brains
SOURCE_SECRET=some-shared-secret-with-another-auxiliary-server-to-prevent-outside-access

# PostgreSQL Configuration (optional - if not set, file cleanup uses filename inference)
# Option 1: Use full connection string
POSTGRES_URL=postgresql://user:password@localhost:5432/upload_metadata

# Option 2: Use individual components (used if POSTGRES_URL not set)
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
POSTGRES_DATABASE=upload_metadata
